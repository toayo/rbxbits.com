import React from "react"

interface Props {
    isOpen: boolean
}

export default class CustomModal extends React.Component {
    constructor(p: Props) {
        super(p)
    }

    render() {
        return <main>

        </main>
    }
}